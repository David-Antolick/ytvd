nodeLinker: node-modules

yarnPath: .yarn/releases/yarn-4.5.1.cjs

# Supply-chain hardening: verify resolution metadata against the registry on
# every install, blocking lockfile-tampering attacks that swap a package's
# fetch URL while leaving its checksum untouched. Auto-on in CI; we want it
# on locally too.
enableHardenedMode: true
